module.exports = {
    name: "purgehack",
    description: "Envoie un message rempli d'espaces invisibles",
    run: async (client, message, args) => {

        setTimeout(() => message.delete().catch(() => {}), 2000);

        const blankMessage = `ﾠﾠ















































































































































































































































































ﾠﾠ`;

        const msg = await message.channel.send(blankMessage);
        setTimeout(() => msg.delete().catch(() => {}), 12000);

    }
}
